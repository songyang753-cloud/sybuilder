# 一页纸操作规程(每阶段:入场→动作→出场门禁)

S2 正式出场还必须经过 `research-quality-gate.py --phase final`。平台由当前计划唯一选择：飞书跑 `feishu-delivery-gate.py`，钉钉跑 `dingtalk-delivery-gate.py`，不能交叉顶替；本地预评审不能代替同版原生页面终审。横比结构门必须携带 `research-package.json`。

S8/S9 正式批准均带 `--context` 绑定本版产物，完整用法见 `references/delivery-quality-contract.md`。

> ⭐ 本表是 `SKILL.md` §【SOP】的详版正文,按**渐进式披露**外移:跑到哪个阶段再读这里对应行,SKILL 主文件只留骨架与指针。
> ⚠️ 出场条件里的编号(①②a②g…)是**历史追加序＝稳定引用键**(有跨文件引用,⛔ 不重排)。
> ⛔ 门禁不过不许进下一阶段。门禁条数/规则数以 `references/design-quality-gates.md` 为准,本表不复述。

| 阶段 | 入场条件 | 核心动作 | 出场门禁（硬性） | 人拍板 |
|---|---|---|---|---|
| **S1** 需求与问题澄清 | — | 用户给方向 + 粗颗粒功能<br>⚠️ **`## 粗颗粒功能` 必须是一个小节，每项以 `**方向名**` 开头** —— `chain-gate G0` 就读这一节做 S1→S2 对账。没有这一节时它退回扫全文 `「」`，**而中文的「」既命名也强调，于是 S1 写得越清楚假漏越多**（2026-09-04 实跑实测）。<br>**已上线产品**：从产品反推定义，**必须交用户确认** | 有 `input/definition.md` | — |
| **S2** 深度研究 | S1 完成；声明 `researchMode=teardown / competitive-pack / full-research / tech-approach` | **共同底线**：真实客户端/网页逐屏遍历，先记 `SURF` 与事件、再归并 `AF`；功能层级不预设四级，而按角色/入口/规则/状态/结果/验收差异继续下钻，直至最细可独立验收单元；每个已验证功能写成正文并就近配真实截图，记录主路径、失败/异常、恢复、字段/规则、反馈与下游输入。`teardown` 依 `s2-teardown-runbook.md` 产单品全景；`competitive-pack` 用同一 AF 并集逐级横比；`full-research` 再完成五条线、三路来源与三份定向报告。**`tech-approach`(技术方案调研:对象=代码实现/技术机制/行业方案,为设计自家方案)例外——不做 UI 遍历,证据=源码真克隆实读+案头三路;开工五项假设表(A1 下游用途/A2 受众/A3 载体账号/A4 深度档位/A5 对象清单=校准门),判据依 `s2-tech-approach-runbook.md`。**Web 用浏览器控制，macOS 客户端须获授权后用 Computer Use，CDP 仅证明结构与行为，不证明视觉。 | 通用：`traversal-coverage-gate.py` 事件覆盖 100%、`report-structure-gate.py --mode ...`、`audience-gate.py`、`feishu-delivery-gate.py` 回读通过；`competitive-pack/full-research` 另过 `research-gate.py`，`full-research` 另过 `chain-gate.py G0`。**`tech-approach` 不适用遍历族门禁,改过 `tech-research-gate.py --pre/--post` + 语义终审。**任何模式不得以目录、树图、截图墙或功能清单替代逐功能正文。 | 否 |
| **S3**（=S3A 业务梳理 → S3B 定义拍板） | S2 完成 | **S3A 先出 `business-map.md`（`business-map-gate.py` 通过）**，再**从 S1 需求 + S2 调研中得出定义**：`definition-final.md`（覆盖下游 18 项）+ **功能增删提案表**；证据不支持则出 `no-build.md` | ①**`definition-gate.py` 退出码 0** ②**三候选互不相同** ③**单向门清单非空** ④**失败路径非空** ⑤**`chain-gate.py G0.5` 通过**（S2 每个 `OPP-xx` 机会都有归宿：采纳/拒绝/推迟，**沉默丢掉不行**）⑤b **`chain-gate.py G0.6` 通过**（high 强度洞察在产品定义有承接） ⑥本阶段必跑 5 视角跑满 ⑦增删每条有 S2 证据 | **是**（最贵的一次拍板） |
| **S4**（=S4A 结构 → S4B PRD v0.9） | S3 已拍板 + **`chain-gate.py G0.9` 通过**（证据停止线已原样抄进 `state.md`——⭐ **产出在 A、执行在 B，没人检查搬运＝强制力为零**） | **S4A 先出 `product-structure.md` + 四张结构图（`product-structure-gate.py` 通过 + **`diagram-id-gate.py` 通过**——图与表 ID 对账三组）**，再写 **PRD v0.9**＝完整骨架（正文九章 + 附件 A–R 结构齐），设计相关章节/字段以 **OPEN 项**登记留空（⭐ 九章填满＝G7.5 回灌后的 **v1.0**，S4 不写设计结论） | ①**`prd_completeness_check.py --stage S4` 退出码 0**（NFR 六类含**可观测性**；⚠️ **S4 允许设计稿列全空**，但会提示「不可直接交研发」）①b **`requirements-quality-gate.py` 退出码 0** ①d **技术方案就绪度 = 可写**（`techspec-readiness.md` T1–T14）①c **`chain-gate.py G0.8` 通过**（定义 18 项在 PRD 有承接位，且**量化与约束级别未被降级**）（含糊词/占位符/术语漂移）②M4b 跨表一致性过 ③本阶段必跑 6 视角跑满、连续两大轮零发现 ④**优先级不得挂 ASM** | **是** |
| **S5** 设计交互 | S4 终审通过 | S5.1 设计方向与令牌**草案** → S5.2 交互规格 → S5.3 评审（⭐ **A 锁评审对象＝渲染出来的方向切片**——HTML/Figma 各 1–2 屏真实内容，不是令牌表；令牌草案在 B 锁代表切片批准后按实况修订，全量铺开前冻结） | ⓪**`design-intent-gate.py` 通过**（A 锁方向评估完成：三方向四维齐或品牌豁免+批准记录；RISK=提案制，零提案须写理由）⓪b **`token-provenance-gate.py` 通过**（令牌逐条有出处）+ **`design-to-tokens.py --check` 通过**（`tokens.json` 是**生成物**，DESIGN.md 的 front matter 才是唯一源 —— 本 SOP 修掉的**第四处双源**）⓪c **`visual-spec-gate.py` 通过**（量化判据；⚠️ 先展开 `var()` 再判）⓪d **`ai-slop-gate.py` 通过**（⚠️ **「通过」＝ A 层无发现**；C 层的字体/配色只报出不阻断——铁律 49）⓪e **`interaction-gate.py` 通过**（交互反模式）⓪g ⭐ **交互实现与五章业务流程图一致**：流程图上的每条**异常边**与**人机切换边**，交互规格里要有对应的状态与反馈；不一致走 **DELTA** 登记，⛔ 不许就地改图了事⓪f **`element-identity-gate.py` 通过**（元素标识表填了，命名按功能）<br>⚠️ 各门禁的规则条数以 `references/design-quality-gates.md` 为准，本表不复述（**复述一次就多一处会过期的副本**）①每个 `F-xx` 有交互说明，且**状态按四类三段式写全**（`interaction-patterns.md` 是唯一权威源；⛔ 本表不复述数量） ②附件 A 双向 ID 绑定 ③控件上限已核文案 ④双端三选一逐功能回答 | **是** |
| **S6** HTML 可执行 UX 基准<br>*(已上线产品跳过)* | S5 的 **A 概念锁已批准**（与 S7 **并行协同**，按 B 代表切片锁 → C 全量锁共同收敛，见架构正本 §四） | `demo-html` 骨架 → 可选叠视觉皮 | ①分发态 bundle 单文件零外链换机可开（开发态=目录版，正本唯一）②0 **先跑 `scenario-matrix-gate.mjs <demo.html> --states spec/states.json --dump .product-flow/demo/anchors.json`** —— ⚠️ **可运行原型的锚点是运行时写在 `body` 上的，静态扫 HTML 一个都取不到**，G2/G3 与 demo-anchor 只会 UNABLE。这一步把**真走到过的**场景导出来当后面三道门的输入；走不到的场景不会出现在 dump 里 —— 「没走到」于是表现为「没有这个场景」，而不是被一张手写表补成「有」。（场景稿式的旧 demo 仍可直接传 `demo.html`，按扩展名自动分流。）②a **`demo-anchor-gate.py <anchors.json|demo.html> --prd <PRD.md>` 通过**（`data-scene`/`data-fr` 锚点齐全）—— ⚠️ **必须排在 G2 之前**：锚点是 G2/G3 的输入，缺了它们只会 UNABLE，而 UNABLE 太容易被当成「跑过了」耸肩带过（2026-08-31 实测 验证项目 的 demo 一个锚点都没有，G2/G3 从未产出过结论）②**`reconcile-gate.py G2 <PRD.md> <anchors.json|demo.html>` 通过**（每个 FR/AC 有场景或「不演示·理由」；⛔ **经 `gate-run.py` 跑以落盘分键记录**——G7.5 ⑤ 读 `reconcile-gate.py.G2.json`，裸跑=NOT-RUN） ②b **`spec-sync-gate.py <spec目录> <demo.html> --js <js目录>` 通过**（规格↔demo 六条对账，含**产物没被手改**）②d **`flow-walk-gate.mjs <demo.html> --flows spec/flows.json` 全部走通** ②g **`mock-seam-gate.mjs <骨架源码目录>` 通过** —— ⚠️ **走可运行原型的必跑**：它验的是骨架**唯一存在的理由** —— 「把 `api.js` 整体换成真 `fetch`，其余三层一行都不用改」。⛔ 这句话不成立，交互稿就不能声称「与真实实现一致」。2026-09-04 实测它**当时是假的**（`P.scenarios` 一删 boot 就抛，页面只剩演示外壳）②f **`platform-parity-gate.mjs <demo.html> --routes … [--declared …]` 通过** —— ⚠️ **双端产品必跑**：它验「PC 形态像 PC app、移动形态像移动 app」中机器能验的那部分 （形式必须不同 + **实质必须相同**）。实证：桌面组件工具箱与移动端框架的组件词汇**只重合 6 个**，一套 DOM 两个断点不可能同时像两端的原生应用（见 `references/platform-parity.md`）②e **`scenario-matrix-gate.mjs <demo.html> --states spec/states.json` 通过**（每个「适用」状态都真的触发得出来）②c **`dead-click-gate.mjs <demo.html> --max-dead-pct 0` 通过**（**死按钮 + 控制台报错**；⭐ 这两项是 S6 自动验收表列了却长期没有实现的两条 —— 实测一份 93% 控件点不动的 demo，视觉门禁判 9 通过 1 失败）③关键路径**真的点通**（读代码不算）③b **`browser-audit.mjs --all-routes --all-viewports --all-themes` 通过**（⛔ **不带 `--all-routes` 只审打开时那一屏**：实测 28 场景的 demo 只审到 1/28，加上后失败项从 1 涨到 5）（静态档替代不了浏览器档；⛔ **双端产品必须带 `--all-viewports`**，否则只跑 1440 一个视口——**移动端等于没审**）③c **`ai-slop-gate.py` / `visual-spec-gate.py` / `interaction-gate.py` / `element-identity-gate.py` 交付前各跑一遍**④自动验收脚本先自证会红 | **是**（锁定） |
| **S7** Figma 视觉母版 | S5 的 **A 概念锁已批准**（与 S6 **并行协同**；⛔ 旧模型「S6 锁定后照 HTML 画」已废 —— G3 对账在 **C 全量锁 / G7.5** 做，届时 HTML 的场景锚点已可导出；**已上线产品＝`baseline.md` 已锁（模板 `templates/baseline.md`：界面清单+行为基线+锁定记录）**）+ **全量铺开前基座库已建**（⭐ B 锁前的**探索切片可直接写值**——真实视觉方向没出现前不锁令牌；⛔ 探索稿不得当正稿交付）+ ⛔ **`state.md` 里没有指向本阶段的未解除停止线**（`evidence-levels.md`）+ ⚠️ **高风险项须先过真人任务验证**（见下） | 见下方 S7 专节 | ⓪**`reconcile-gate.py G3`（⛔ 经 `gate-run.py` 跑以落盘分键记录，裸跑=NOT-RUN）与 `doc-sync-guard.py figma-anchors` 通过**（设计稿链接必须 node-id + 图层名**双锚**，node-id 重建后会失效）①**无基座不许铺全量**（顺序＝探索切片 → B 锁批准 → 从切片提炼令牌建基座 → 全量铺开全绑实例/变量） ①b **`figma-editability-gate.py` 每批写入后跑** ①c **`element-identity-gate.py` 定稿后跑** ②全部为组件实例+绑定变量（`containsBitmapFill:false`）③按载体出对应端 ④链接回灌 PRD 第四章 | **是**（定稿） |
| **G7.5** 三方冻结 | A/B/C 三锁齐 | PRD v1.0 回灌（附件 M）+ 三方对账 | **`g75-freeze-gate.py <项目根>` 通过**（本地六查+原生三查；报告 `templates/triad-reconciliation.md`） | **是**（冻结） |
| **S8** 技术、算法与测试方案 | **G7.5 三方冻结通过**；原生 PRD/Figma/HTML 与 spec 可读；`techspec-readiness`、`testcases-readiness` 给出结论；项目规范、`coding-standards`、`four-node-review` 均有路径+commit/hash；独立运行时按 Gap 三分法，不伪造 G7.5 | 研发/算法/测试三轨起草 → 可编辑技术架构图 → 模块接口/数据/存储/性能容量先闭合 → `FR/NFR/AC↔TECH↔ALG/N-A↔TEST↔STD↔VS` 对账 → 规划 S9.2 测试工程师与 S9.3 产品经理两次独立 GUI 实走 → 配 four-node 风险建议档与终审预案 → 三方交叉质询 → 飞书原生写入与回读；模板 `templates/s8-solution-plan.md`，细则 `references/s8-solution-plan.md` | ①**`s8-solution-gate.py` 十一判据退出码 0** ②技术/算法或 N/A/测试三章完整，七类测试与 GUI 双角色实走显式成章 ③技术架构图、模块、接口、数据、存储、性能均有 ID 与锚点 ④双向追溯无缺口或孤儿项 ⑤产品 delta 已回 G7.5 重新批准 ⑥飞书 URL/revision/receipt 回读在案 ⑦三专业分别批准 | **是**（三专业分别批准） |
| **S9** 研发·审计·验收·上线（S9.1–S9.4） | **S8 联合方案 APPROVED**；⭐ 用例覆盖**业务流程图每条异常边**与**每条 AI 降级/转人工路径**；⛔ 无未解除停止线；模式已定（Handoff / Build，默认 Handoff）；动手写代码前读取 S8 锚定的 `coding-standards`，S9.2 读取锚定的 `four-node-review`；`references/s9-quality-gates.md` 只定义接入与兜底 | S9.1 按纵向切片和 `STD-ID` 开发、RED→GREEN、持续验证 → S9.2 测试工程师以 GUI 亲自完整实走当前构建，再人工定 S/A/B/C、建真实 diff 覆盖合同、跑 entry/gates/security/review/qa/bench、证伪与全量回归，写飞书终审报告并得出 `PASS` → S9.3 产品经理基于冻结 PRD 与产品经验另开会话独立 GUI 走查、做四方验收并写飞书报告 → S9.4 灰度/正式上线；偏离 S8 方案时同 commit 定位更新飞书与受控镜像 | ⓪Build 模式用解析到的真实 `coding-standards/scripts/selfcheck.sh` 经 `gate-run.py` 落盘，缺能力记 UNABLE；⚠️ selfcheck 不证明代码合规 ①七类测试逐项有结论，需求/行/分支/变异覆盖分报 ②`coverage_check.py` 与 **`s9-quality-report-gate.py`** 均通过，测试工程师 GUI 实走当前构建且质量结论 PASS ③four-node 必跑节点按档位连续 CLEAN、每轮包 m/m 且独立新证据 ④没真跑记 SKIPPED/UNABLE，永不算 PASS ⑤SEC-1~10 + `sec-scan.py` + 产品级安全依据 ⑥S9.3 产品经理亲自 GUI 实走，`s9-product-walkthrough-gate.py` 通过，四方分别对账且未批准高影响偏差=0 ⑦两次 GUI 证据、会话与角色独立，不能互相顶替 ⑧灰度停止线、监测、恢复与上线批准在案 | **是**（质量/产品/上线分别拍板） |
| **S10** 上线后复盘 | 上线满一个观察周期（**周期怎么定见 `s10-retro.md` 一节**） | **先给决定**（继续/扩大/调整/停止）+ 四本账结算 | ⓪**`retro-gate.py` 退出码 0** ①3.2 每个目标三态（达成/未达成/**读不出来**）①b ⭐ 含 AI 时：**线上实测效果 vs 附件 A.4 的阈值**逐条对账（达标/未达标/**测不出来**） ②每条 `ASM-###` 标「成立/推翻/仍未知」+ 当初的证据等级 ③逃逸缺陷已回流 `lessons.md` ④**决策质量账**：回头看 S3 那次拍板 ⑤因果性诚实声明 | **是**（结论） |
