# B 代表切片锁 · 切片登记表（`delivery-pipeline.md` §四）

> 按风险选 1–3 个代表切片；四个覆盖类型**必须都有着落**（一个切片可覆盖多个类型）。
> 同一切片在 Figma 与 HTML 必须用**同一份真实内容、同一 FR/AC/SCN/EL、同一状态与端声明**。
> ⛔ 覆盖类型有空缺、或两侧锚点缺一侧，B 锁不算过（`g75-freeze-gate` 会查本表）。

## 切片清单

| 切片 | 覆盖类型（可多选：核心路径/最难一屏/异常恢复/端差异） | SCN | FR/AC | 内容 fixture | Figma node | HTML scene(route+scn) | 状态与端声明 |
|---|---|---|---|---|---|---|---|
| 切片 1 <名> | | | | FIX- | | | |

## 覆盖类型对账（四行必须全非空）

| 覆盖类型 | 由哪个切片覆盖 |
|---|---|
| 最核心的完成路径 | |
| 信息密度/层级最难的一屏 | |
| 一个错误/空态/权限/恢复场景 | |
| （双端时）体现端差异的场景；单端产品写 N/A+「单端」 | |

## B 锁验收记录

- [ ] HTML 侧：切片流程**实际操作**走通（不是读代码）—— 操作人 <__> 日期 <__>
- [ ] Figma 侧：对照 HTML 同内容做过视觉审查（`design-quality-gates.md`「人工审美评审表」13 维逐维给证据）—— 评审人 <__> 日期 <__>
- [ ] 高风险决定已做目标用户/代表性使用者验证，或显式记 deferred+理由
- **批准记录**：<谁> 于 <日期> 批准进入 C 全量覆盖
