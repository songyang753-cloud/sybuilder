# 信息架构 / 站点地图（示例）— 对象：product-flow 的文档体系，对应 PRD 6.3
# 判据见 references/diagram-standards.md 第 4 节（Rosenfeld & Morville 四系统）。
# ⛔ 节点 ID 只能是 P-xx 一族。
# ⚠️ 站点地图 ≠ 信息架构：地图只是 IA 四系统里**组织系统**的那一张投影。
#    另外三个系统（标签 / 导航 / 检索）在图上要有落点，否则这张图只回答了四分之一。

direction: right
title: |md # 信息架构（示例）：组织 · 标签 · 导航 · 检索 | {near: top-center}

组织系统: {
  style.fill: "#EEF2FF"
  P-01: "P-01 入口\nSKILL.md"
  P-02: "P-02 阶段说明\nS1–S10 + G7.5"
  P-03: "P-03 门禁清单\ndesign-quality-gates.md"
  P-04: "P-04 模板库\ntemplates/"
  P-05: "P-05 决策记录\ndocs/adr/"
  P-01 -> P-02
  P-01 -> P-03
  P-01 -> P-04
  P-01 -> P-05
}

标签系统: {
  style.fill: "#F0FDF4"
  标: "统一记号：⛔ 硬约束 / ⚠️ 易错 / ⭐ 关键判断\n⇒ 同一个词在九章里只有一种含义"
}

导航系统: {
  style.fill: "#FEF9C3"
  导: "全局：SKILL.md 目录\n局部：每份 reference 顶部的「本文验不了什么」\n上下文：门禁红条目直链到章节"
}

检索系统: {
  style.fill: "#FDF2F8"
  检: "grep 友好：判据句带固定前缀\n门禁名 = 可检索的稳定 ID"
}

标签系统.标 -> 组织系统.P-03: 记号的权威定义在此
导航系统.导 -> 组织系统.P-01: 全局导航挂在入口
检索系统.检 -> 组织系统.P-03: 门禁名即检索键
组织系统.P-05 -> 组织系统.P-03: "为什么是这道门（ADR 反向索引）"

# ⚠️ 这张图答不了：每一页长什么样（那是交互稿/视觉稿的事）、
#    页面之间用户实际怎么走（那是四章用户流程图的事）。
# ⛔ 也答不了内容的**生命周期** —— 谁维护、多久过期，那是附件 C.5。
