# 用户流程图（示例）— 对象：产品经理用 product-flow 产出一份 PRD，对应 PRD 4.3
# 判据见 references/diagram-standards.md 第 7 节。
# ⛔ 主语是**一个用户**，不用泳道 —— 用泳道就变成五章的业务流程图了。
# ⛔ 本图**不编 M/F/P 号**：4.3 的步骤是行为不是功能条目，
#    硬编号会造出第二套编码体系（diagram-id-gate 因此不对账这张图，是刻意的）。

direction: down
title: |md # 用户流程图（示例）：产品经理产出一份 PRD | {near: top-center}

图例: "👤 用户做的　｜　⚙️ 系统/AI 做的\n（判定清单第 6 条：两种节点必须能区分，⛔ 只靠颜色不算）" {style.fill: "#F8FAFC"; style.stroke-dash: 2}

入口A: "入口①：新项目立项" {shape: circle; style.fill: "#EEF2FF"}
入口B: "入口②：老 PRD 要改" {shape: circle; style.fill: "#EEF2FF"}

选档: "👤 填写前先定档位\n(轻档 / 全档)" {style.fill: "#F0FDF4"}
填章: "👤 逐章填写\n(九章 + 附件)" {style.fill: "#F0FDF4"}
跑门: "⚙️ 跑门禁" {style.fill: "#F0FDF4"}

判: "◆ 全绿？" {shape: diamond}
# ⛔ 两条分支都要画 —— 只画 yes 不画 no 是最常见的缺口
改: "👤 按红条目回到对应章节" {style.fill: "#FEF3C7"}

# ⭐ AI 追加档三件：不确定分支 / 人工接管 / 纠错回路
AI判: "◆ AI 能不能取到冻结证据？" {shape: diamond; style.fill: "#FDF2F8"}
AI好: "⚙️ AI 结构化产出证据" {style.fill: "#FDF2F8"}
人工: "👤⇄ 人工接管：自己贴证据链接" {style.fill: "#FDF2F8"}
纠错: "👤 结果不对 → 标注纠正" {style.fill: "#FDF2F8"}

会签: "👤 提交三方会签" {style.fill: "#FEF9C3"}
判2: "◆ 会签通过？" {shape: diamond}
成: "终：PRD 冻结（成功路径终点）" {shape: circle; style.fill: "#DCFCE7"}
# ⛔ 失败/放弃不许悬空 —— 悬空就是在图上说「用户消失了」
弃: "终：放弃本轮\n(存草稿，下次从填章继续)" {shape: circle; style.fill: "#FEE2E2"}

入口A -> 选档
入口B -> 填章: "已有内容，跳过定档"
选档 -> 填章
填章 -> 跑门
跑门 -> 判
判 -> 改: 否
改 -> 跑门: "回环①：改完重跑"
判 -> AI判: 是
AI判 -> AI好: 能
# ⛔ 不确定分支：AI 取不到时用户看到什么，必须画
AI判 -> 人工: "不能 / 置信度低" {style.stroke-dash: 3}
AI好 -> 纠错: 用户发现结果不对
纠错 -> AI好: "回环②：纠正后重新生成"
AI好 -> 会签
人工 -> 会签
会签 -> 判2
判2 -> 成: 通过
判2 -> 改: "驳回（回环③）"
# ⚠️ 流失点：最可能在这里放弃 —— 画不出流失点说明还在画理想路径
改 -> 弃: "⚠️ 主要流失点：反复返工后放弃"

# ⚠️ 这张图答不了：岔路口设得合不合理（只证明岔路画全了）、
#    真实流失率（上面那个流失点是判断不是数据，有埋点就用数据推翻它）。
